# Lista de números
numeros = [1, 2, 3, 4, 5]

# Usando la,bda com map para calcular o quadrado de cada número
quadrados = list(map(lambda x: x ** 2, numeros))

print(quadrados) # Saída: [1, 4, 9, 16, 25]